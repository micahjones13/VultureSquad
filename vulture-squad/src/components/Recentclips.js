import React from "react";
import styled from "styled-components";

const RecentClips = props => {
  return (
    <div>
      <h2>Recent clips here</h2>
    </div>
  );
};

export default RecentClips;
